[package]
name = "andamento-core"
version = "0.1.0"
edition = "2021"
license = "MIT"

[features]
test-support = []

[dependencies]
kdl = { version = "4.7.1", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
unicode-width = "0.1.14"

[dev-dependencies]
insta = "1"
