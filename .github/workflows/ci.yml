name: CI

on:
  pull_request:
    branches: [main]
  merge_group:
  push:
    branches: [main]

permissions:
  contents: read

env:
  ZELLIJ_REVISION: d262df58ebdea2549666a5060663d017545bb5cb

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/checkout@v7
        with:
          repository: rjwittams/zellij
          ref: ${{ env.ZELLIJ_REVISION }}
          path: zellij
      - name: Link sibling Zellij dependency
        run: ln -s "$GITHUB_WORKSPACE/zellij" "$(dirname "$GITHUB_WORKSPACE")/zellij"
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: >-
          cargo fmt --check
          --package andamento
          --package andamento-shared
          --package andamento-controller
          --package andamento-rail
          --package andamento-config
          --package andamento-core
          --package andamento-terminal
          --package andamento-html
          --package andamento-ffi
          --package andamento-tui

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    needs: fmt
    steps:
      - uses: actions/checkout@v7
      - uses: actions/checkout@v7
        with:
          repository: rjwittams/zellij
          ref: ${{ env.ZELLIJ_REVISION }}
          path: zellij
      - name: Link sibling Zellij dependency
        run: ln -s "$GITHUB_WORKSPACE/zellij" "$(dirname "$GITHUB_WORKSPACE")/zellij"
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-wasip1
          components: clippy
      - uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-clippy-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-clippy-
      - run: cargo clippy --workspace --target wasm32-wasip1 --locked

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: fmt
    steps:
      - uses: actions/checkout@v7
      - uses: actions/checkout@v7
        with:
          repository: rjwittams/zellij
          ref: ${{ env.ZELLIJ_REVISION }}
          path: zellij
      - name: Link sibling Zellij dependency
        run: ln -s "$GITHUB_WORKSPACE/zellij" "$(dirname "$GITHUB_WORKSPACE")/zellij"
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-test-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-test-
      - run: >-
          cargo test --workspace --lib --target x86_64-unknown-linux-gnu --locked

  wasm:
    name: WASM build
    runs-on: ubuntu-latest
    needs: fmt
    steps:
      - uses: actions/checkout@v7
      - uses: actions/checkout@v7
        with:
          repository: rjwittams/zellij
          ref: ${{ env.ZELLIJ_REVISION }}
          path: zellij
      - name: Link sibling Zellij dependency
        run: ln -s "$GITHUB_WORKSPACE/zellij" "$(dirname "$GITHUB_WORKSPACE")/zellij"
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-wasip1
      - uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-wasm-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-wasm-
      - run: cargo build --workspace --target wasm32-wasip1 --release --locked -j1

  independent:
    name: Core and frontends without Zellij
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - run: python3 scripts/check-independent-core.py
